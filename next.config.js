
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // remove experimental.appDir - App Router is enabled by default in modern Next.js
};

module.exports = nextConfig;

